# Mission 3: Variable Scope, Shadowing, and Preconditions - 정답 가이드

이 문서는 **Mission 3: 사라진 4,000벨의 행방**의 모든 단계에 대한 정답 코드와 간단한 설명을 포함하고 있습니다.

---

## 📚 Chapter 1: 사라진 4,000벨 (Scope)
### 상황
플레이어가 농어를 팔아 4000벨을 벌었지만, 상점을 나가자마자 돈이 사라졌습니다. 이는 돈을 `지역 변수(myBells)`에 저장했기 때문입니다. 지역 변수는 메소드 실행이 끝나면 메모리에서 삭제됩니다.

### 목표
지역 변수 `myBells`를 **인스턴스 변수(필드)**로 옮겨서 데이터가 영구히 유지되도록 하세요.

### 정답 코드
```java
public class User {
    // 인스턴스 변수 선언 (클래스 레벨)
    // private 접근 제어자를 사용하는 것이 좋은 습관입니다.
    private int myBells = 0; 
    
    public void sellFish() {
        // 지역 변수 선언(int myBells = 4000;)을 제거하고,
        // 인스턴스 변수(this.myBells)에 값을 더합니다.
        this.myBells += 4000;
        
        System.out.println("4000벨 획득!");
    }
}
```

---

## 📚 Chapter 2: 도플갱어의 습격 (Shadowing)
### 상황
대출금을 갚으려는데 돈이 줄어들지 않고 0원이 되거나 이상하게 동작합니다. 매개변수 이름(`myBells`)과 인스턴스 변수 이름(`myBells`)이 같아서, 컴퓨터가 **가장 가까운 매개변수를 우선**하여 사용했기 때문입니다(Variable Shadowing).

### 목표
`this` 키워드를 사용하여 "내 주머니(인스턴스 변수)"를 명확하게 가리켜서 올바르게 차감되도록 하세요.

### 정답 코드
```java
public class User {
    private int myBells = 4000;
    
    public void payLoan(int amount) { // 매개변수 이름을 바꾸는 것도 방법이지만, 문제의 의도는 Shadowing 해결입니다.
        // this.myBells는 인스턴스 변수를, amount(또는 매개변수 myBells)는 입력받은 값을 가리킵니다.
        this.myBells -= amount;
        
        // 만약 매개변수 이름이 myBells라면:
        // this.myBells -= myBells; (이렇게 해야 합니다)
    }
}
```
**참고:** 코드 템플릿상 매개변수를 `amount`로 바꾸는 것도 정답으로 인정되지만, 핵심은 `this.myBells`에 값을 할당하거나 차감해야 한다는 점입니다.

---

## 📚 Chapter 3: 마이너스 통장 버그 (Precondition)
### 상황
돈이 없는데도 물건이 구매되어 잔액이 마이너스가 됩니다. 현실에서는 돈을 확인하고 물건을 주지만, 코드에는 그 **확인 절차(Precondition)**가 없기 때문입니다.

### 목표
구매를 진행하기 전에 `if` 문을 사용하여 **잔액이 충분한지 확인**하고, 부족하면 구매를 막으세요.

### 정답 코드
```java
public class User {
    private int myBells = 1000;
    
    public void buyTool(int cost) {
        // 사전 조건(Precondition): 돈이 충분한가?
        if (this.myBells >= cost) {
            // 충분하면 차감 및 구매
            this.myBells -= cost;
            System.out.println("구매 완료!");
        } else {
            // 부족하면 거절
            System.out.println("잔액이 부족합니다.");
        }
    }
}
```

---

## 📚 Chapter 4: 최종 미션 - 품절입니다! (Advanced Precondition)
### 상황
돈은 충분하지만, 재고가 없는 물건을 살 수 있는 문제가 있습니다. 상점 시스템에 `inStock` 변수가 추가되었습니다.

### 목표
`inStock` 변수를 확인하여, **재고가 없으면(false)** "품절입니다!"를 출력하고 구매를 막으세요. 돈과 재고 두 가지 조건을 모두 통과해야 구매가 가능합니다.

### 정답 코드
```java
public class User {
    private int myBells = 2000;
    private boolean inStock = false; // 재고 없음
    
    public void buyTool(int cost) {
        // 1. 돈이 충분한지 확인
        if (this.myBells >= cost) {
            // 2. 재고가 있는지 확인 (중첩 if문)
            if (this.inStock) {
                this.myBells -= cost;
                System.out.println("구매 완료!");
            } else {
                System.out.println("품절입니다!");
            }
        } else {
            System.out.println("잔액이 부족합니다.");
        }
    }
}
```
**팁:** 논리 연산자(`&&`)를 사용하여 `if (this.myBells >= cost && this.inStock)` 형태로 한 번에 묶을 수도 있습니다.

---
**작성일:** 2025-12-18
**작성자:** Antigravity (Assistant)
