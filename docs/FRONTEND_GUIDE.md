# 🎨 프론트엔드 구현 가이드
## Animal Forest Coding - React 사용자 인터페이스

**버전**: 0.1.0
**상태**: ✅ 완성
**마지막 업데이트**: 2024-11-30

---

## 📋 목차

1. [개요](#개요)
2. [설치 및 실행](#설치-및-실행)
3. [프로젝트 구조](#프로젝트-구조)
4. [주요 컴포넌트](#주요-컴포넌트)
5. [커스텀 훅](#커스텀-훅)
6. [동작 흐름](#동작-흐름)
7. [기능](#기능)

---

## 개요

React 기반의 사용자 인터페이스로, 백엔드 API와 연결되어 다음 기능을 제공합니다:

✅ **캐릭터 선택** - 6개의 동물 숲 캐릭터 선택
✅ **학습 콘텐츠 표시** - 선택한 캐릭터의 학습 자료 표시
✅ **이미지 표시** - 학습 콘텐츠의 관련 이미지 표시
✅ **Animalese 음성** - 캐릭터의 음성으로 콘텐츠 자동 재생
✅ **오디오 플레이어** - 음성 재생/일시정지 제어

---

## 설치 및 실행

### 필수 요구사항
- Node.js 16.x 이상
- npm 7.x 이상
- 백엔드 서버 실행 중 (http://localhost:5000)

### 설치 및 실행 명령어

```bash
# 프로젝트 디렉토리로 이동
cd frontend

# 의존성 설치
npm install

# 개발 서버 시작 (http://localhost:3000)
npm start

# 프로덕션 빌드
npm run build

# 테스트 실행
npm test
```

---

## 프로젝트 구조

```
frontend/
├── public/
│   └── index.html              # HTML 진입점
├── src/
│   ├── components/
│   │   ├── CharacterSelector.tsx   # 캐릭터 선택 컴포넌트
│   │   ├── CharacterSelector.css
│   │   ├── ContentDisplay.tsx      # 콘텐츠 표시 컴포넌트
│   │   ├── ContentDisplay.css
│   │   ├── AudioPlayer.tsx         # 오디오 플레이어 컴포넌트
│   │   └── AudioPlayer.css
│   ├── hooks/
│   │   ├── useContent.ts           # 콘텐츠 로딩 훅
│   │   └── useAudio.ts             # 음성 생성/재생 훅
│   ├── services/
│   │   └── apiClient.ts            # API 클라이언트
│   ├── App.tsx                     # 메인 앱 컴포넌트
│   ├── App.css
│   ├── index.tsx                   # React 진입점
│   └── index.css
├── package.json
├── tsconfig.json
├── .env.example
└── .gitignore
```

---

## 주요 컴포넌트

### 1. CharacterSelector
**역할**: 사용 가능한 모든 캐릭터를 그리드 형태로 표시하고 선택 가능

**props**:
```typescript
{
  characters: Character[]        // 캐릭터 목록
  selectedCharacter?: string     // 선택된 캐릭터
  onSelect: (name: string) => void  // 선택 콜백
  loading?: boolean             // 로딩 상태
}
```

**특징**:
- ✅ 6개 캐릭터 카드 표시
- ✅ 호버 효과 및 선택 표시
- ✅ 키보드 접근성 지원
- ✅ 반응형 디자인

### 2. ContentDisplay
**역할**: 선택된 캐릭터와 주제의 학습 콘텐츠 표시

**props**:
```typescript
{
  content: Content | null       // 콘텐츠 데이터
  loading?: boolean            // 로딩 상태
  error?: Error | null         // 에러 메시지
}
```

**특징**:
- ✅ 콘텐츠 텍스트 표시
- ✅ 관련 이미지 표시
- ✅ 난이도 및 예상 시간 표시
- ✅ 자동 음성 생성 (콘텐츠 변경 시)
- ✅ AudioPlayer 통합

### 3. AudioPlayer
**역할**: Animalese 음성 재생/제어

**props**:
```typescript
{
  audioUrl: string              // 음성 파일 URL
  character: string             // 캐릭터 이름
  loading?: boolean             // 음성 생성 중
  isPlaying?: boolean           // 재생 중
  onPlay: () => void            // 재생 클릭
  onStop: () => void            // 일시정지 클릭
  audioRef: RefObject<HTMLAudioElement>
  onAudioEnded: () => void      // 음성 재생 완료
  error?: Error | null          // 에러 메시지
}
```

**특징**:
- ✅ 재생/일시정지 버튼
- ✅ 진행률 바 및 시간 표시
- ✅ 카타고리별 스타일링 (자주색 그래디언트)
- ✅ 음성 생성 로딩 상태 표시

---

## 커스텀 훅

### useContent
**역할**: 특정 캐릭터와 주제의 콘텐츠 로드

```typescript
const { content, loading, error } = useContent(character, topic);
```

**반환 값**:
```typescript
{
  content: Content | null    // 로드된 콘텐츠
  loading: boolean          // 로딩 상태
  error: Error | null       // 에러 메시지
}
```

**동작**:
1. character 또는 topic이 변경되면 API 요청
2. 로딩 상태 관리
3. 에러 처리
4. 콘텐츠 캐싱

### useAudio
**역할**: Animalese 음성 생성 및 재생 관리

```typescript
const {
  audioUrl,
  audioRef,
  loading,
  error,
  isPlaying,
  generateAudio,
  playAudio,
  stopAudio,
  handleAudioEnded
} = useAudio();
```

**반환 값**:
```typescript
{
  audioUrl: string                          // 생성된 음성 URL
  audioRef: RefObject<HTMLAudioElement>    // 오디오 요소
  loading: boolean                          // 생성 중
  error: Error | null                      // 에러
  isPlaying: boolean                        // 재생 중
  generateAudio: (text, character) => Promise<string | null>
  playAudio: (url: string) => void
  stopAudio: () => void
  handleAudioEnded: () => void
}
```

**동작**:
1. TTS API로 음성 생성
2. AudioPlayer의 audio 요소와 연결
3. 재생/일시정지 제어
4. 진행률 추적

---

## 동작 흐름

### 페이지 로드 시

```
1. App 마운트
   ↓
2. 백엔드에서 캐릭터 목록 로드
   ↓
3. CharacterSelector에 표시
   ↓
4. 첫 번째 캐릭터 자동 선택
   ↓
5. ContentDisplay에 콘텐츠 로드 요청
```

### 캐릭터 선택 시

```
1. CharacterSelector에서 클릭
   ↓
2. selectedCharacter 상태 업데이트
   ↓
3. useContent 훅 트리거
   ↓
4. 백엔드에서 새 콘텐츠 로드
   ↓
5. ContentDisplay에 표시
   ↓
6. 콘텐츠 변경 감지 → useAudio의 generateAudio 호출
   ↓
7. TTS API로 음성 생성
   ↓
8. AudioPlayer에 URL 전달
```

### 재생 버튼 클릭 시

```
1. AudioPlayer의 재생 버튼 클릭
   ↓
2. useAudio의 playAudio() 호출
   ↓
3. HTMLAudioElement.play() 실행
   ↓
4. 오디오 재생 시작
   ↓
5. 진행률 바 및 시간 표시 업데이트
   ↓
6. 재생 완료 시 onAudioEnded 콜백
```

---

## 기능

### ✅ 사용자 인터페이스

| 기능 | 설명 |
|------|------|
| 캐릭터 그리드 | 6개 캐릭터를 클릭으로 선택 |
| 주제 선택 | 드롭다운으로 7개 주제 선택 |
| 콘텐츠 표시 | 선택한 캐릭터의 학습 자료 표시 |
| 이미지 표시 | 학습 관련 이미지 표시 |
| 음성 플레이어 | 재생/일시정지, 진행률 표시 |

### ✅ 상태 관리

| 상태 | 설명 |
|------|------|
| selectedCharacter | 현재 선택된 캐릭터 |
| selectedTopic | 현재 선택된 주제 |
| characters | 로드된 캐릭터 목록 |
| content | 로드된 콘텐츠 |
| audioUrl | 생성된 음성 URL |
| isPlaying | 음성 재생 중 여부 |

### ✅ API 통합

| 엔드포인트 | 용도 |
|-----------|------|
| GET /api/characters | 캐릭터 목록 조회 |
| GET /api/content/:character/:topic | 콘텐츠 조회 |
| GET /api/images/:imageId | 이미지 조회 |
| POST /api/tts | Animalese 음성 생성 |

---

## 스타일링

### 색상 스키마
```
주색:     #667eea (보라색)
부색:     #764ba2 (진한 보라)
강조:     #4CAF50 (녹색)
배경:     #f5f7fa (연한 파란)
```

### 반응형 디자인
```
데스크톱:  1200px 이상 (2열 레이아웃)
태블릿:    768px ~ 1199px (1.5열 레이아웃)
모바일:    480px ~ 767px (1열 레이아웃)
스몰:      480px 이하 (전체폭)
```

---

## 에러 처리

### 캐릭터 로드 실패
```
표시: ❌ 캐릭터를 불러올 수 없습니다: [에러 메시지]
해결: 백엔드 서버 연결 확인
```

### 콘텐츠 로드 실패
```
표시: 콘텐츠를 로드할 수 없습니다
해결: 유효한 캐릭터/주제 확인
```

### 음성 생성 실패
```
표시: AudioPlayer에 에러 메시지 표시
해결: 백엔드 TTS 서비스 확인, 속도 제한 확인
```

---

## 개발 팁

### 디버깅
```javascript
// API 응답 확인
console.log(response.data);

// 상태 변경 추적
useEffect(() => {
  console.log('Character changed:', selectedCharacter);
}, [selectedCharacter]);

// 에러 로깅
catch (err) {
  console.error('Error:', err);
}
```

### 성능 최적화
```javascript
// 메모이제이션
import { memo, useMemo, useCallback } from 'react';

const CharacterCard = memo(({ character, onSelect }) => {
  return (
    <div onClick={() => onSelect(character.name)}>
      {character.name}
    </div>
  );
});
```

### 접근성 개선
```typescript
// 키보드 네비게이션
onKeyDown={(e) => {
  if (e.key === 'Enter' || e.key === ' ') {
    onSelect(character.name);
  }
}}

// ARIA 레이블
<button aria-label="재생" />
```

---

## 배포

### 환경 변수 설정
```bash
# .env 파일 생성
REACT_APP_API_BASE_URL=https://api.yourdomain.com
```

### 빌드
```bash
npm run build
# build/ 폴더에 프로덕션 빌드 생성
```

### 배포 옵션
- **Vercel**: `npm install -g vercel` → `vercel`
- **Netlify**: Drag & drop build folder
- **AWS S3 + CloudFront**: `aws s3 sync build/ s3://bucket-name`

---

## 시스템 요구사항

| 항목 | 최소 | 권장 |
|------|------|------|
| Node.js | 16.x | 18.x+ |
| npm | 7.x | 8.x+ |
| 브라우저 | 모던 브라우저 | 최신 버전 |
| 백엔드 | 실행 중 | 포트 5000 |

---

## 문제 해결

### 포트 3000이 이미 사용 중
```bash
# macOS/Linux
lsof -i :3000
kill -9 <PID>

# Windows
netstat -ano | findstr :3000
taskkill /PID <PID> /F
```

### 백엔드 연결 안 됨
```bash
# 백엔드 서버 실행 확인
curl http://localhost:5000/api/health

# 응답 예:
# {"status":"healthy",...}
```

### CORS 에러
```
해결:
1. 백엔드 CORS 설정 확인
2. 환경 변수 REACT_APP_API_BASE_URL 확인
3. 백엔드 재시작
```

---

## 다음 단계

- [ ] 사용자 인증 추가 (로그인/회원가입)
- [ ] 학습 진행률 추적
- [ ] 더보기 기능 (페이지네이션)
- [ ] 다크 모드
- [ ] 언어 선택 (영어, 일본어 등)
- [ ] 모바일 앱 (React Native)

---

**작성자**: AI Assistant
**라이선스**: MIT
**관련 문서**: [README.md](../README.md), [API.md](../docs/API.md)
