# 🗺️ 구현 로드맵 (Implementation Roadmap)

**문서**: 기술 고도화 전략 시리즈 #3
**작성일**: 2025-12-04
**목표**: 구체적 구현 계획 및 일정

---

## 📅 전체 타임라인

```
┌─ Phase 3.0: 준비 (1주)
│  ├─ 모듈형 에셋 리스트 확정
│  ├─ 너굴 AI 페르소나 작성
│  └─ 기술 스택 선정
│
├─ Phase 3.1: MVP 구현 (2주)
│  ├─ 모듈형 에셋 시스템
│  ├─ 뉴로-너굴 AI 기본 버전
│  └─ Episode 1 통합
│
├─ Phase 3.2: 고도화 (1주)
│  ├─ 성능 최적화
│  ├─ 캐싱 구현
│  └─ 사용자 테스트
│
└─ Phase 3.3: 확장 (2주)
   ├─ Episode 2 제작
   ├─ 에러 타입 확대
   └─ 문제 해결
```

**총 소요시간**: 6주 (완전 자동화 가능)

---

## 🎯 Phase 3.0: 준비 (1주)

### Week 1: 설계 및 리소스 준비

**목표**: 모든 고도화 작업의 기초 마련

#### 작업 1: 에셋 리스트 확정 ✓

```
배경 이미지 (5장):
┌─────────────────────────────────────────┐
│ 1. bg_island_daytime (512KB)            │ ← 현재 완성
│ 2. bg_island_night (512KB)              │
│ 3. bg_island_shop (512KB)               │
│ 4. bg_island_forest (512KB)             │
│ 5. bg_island_public (512KB)             │
└─────────────────────────────────────────┘

캐릭터 스프라이트:
┌─────────────────────────────────────────┐
│ 너굴 (5가지 표정)                       │
│ - neutral, explain, surprised, happy,   │
│   greedy                                │
│ 크기: 512x512 × 5 = 2560x512            │
│                                         │
│ 플레이어 (3가지 포즈)                   │
│ - normal, coding, success               │
│ 크기: 512x512 × 3 = 1536x512            │
│                                         │
│ NPCs (2가지)                            │
│ - 로드리, 곰돌이 등 (향후 추가)         │
└─────────────────────────────────────────┘

UI 요소 (8장):
┌─────────────────────────────────────────┐
│ - dialogue_box.png                      │
│ - banner_success.png                    │
│ - banner_error.png                      │
│ - mission_window.png                    │
│ - code_editor_frame.png                 │
│ - progress_bar_frame.png                │
│ - button_play.png                       │
│ - button_reset.png                      │
└─────────────────────────────────────────┘

총 에셋 규모: ~4MB (로드 시간: <2초)
```

**담당**: 디자인팀
**완료 조건**: 모든 이미지 PNG 형식, 512px 이상 해상도

#### 작업 2: 너굴 AI 페르소나 작성 ✓

이미 `05_NOOK_PERSONA_PROMPT.md`에 작성됨 (docs 폴더의 다른 문서)

**필요 내용**:
- [ ] 기본 System Prompt 확정
- [ ] 5가지 주요 에러 타입별 피드백 스크립트
- [ ] 성공 시 칭찬 배리에이션 (5가지)
- [ ] 진행도별 응원 메시지
- [ ] 이모지 및 톤 가이드라인

**담당**: 교육 콘텐츠팀
**완료 조건**: 프롬프트 검토 및 승인

#### 작업 3: 기술 스택 선정 ✓

```typescript
// 프론트엔드
- React: UI 컴포넌트 (이미 사용 중)
- Canvas API: 에셋 렌더링
- TypeScript: 타입 안전성

// 백엔드
- Node.js/Express: API 서버 (이미 사용 중)
- Claude API: AI 피드백
- Redis: 응답 캐싱

// 데이터베이스
- PostgreSQL: 사용자 진행도 (선택사항)
- In-Memory: MVP 단계에서는 불필요

// 배포
- Docker: 컨테이너화
- AWS/GCP: 클라우드 호스팅
```

**담당**: 아키텍처팀
**완료 조건**: 기술 선택 최종 결정

---

## 💻 Phase 3.1: MVP 구현 (2주)

### Week 2-3: 핵심 시스템 구현

#### Sprint 1: 모듈형 에셋 시스템 (1주)

**목표**: 에셋 시스템 구현 및 기존 story.html 통합

**Task 1.1: AssetManager 클래스 작성**
```typescript
// 파일: frontend/src/core/AssetManager.ts
class AssetManager {
  async loadAsset(name: string, path: string): Promise<void>
  async loadMultiple(assetList: AssetInfo[]): Promise<void>
  getAsset(name: string): HTMLImageElement
  async preloadAllAssets(): Promise<void>
}

// 테스트:
// ✓ 모든 에셋 로드 성공
// ✓ 로드 시간 < 2초
// ✓ 메모리 사용 < 100MB
```

**Task 1.2: SceneComposer 클래스 작성**
```typescript
// 파일: frontend/src/core/SceneComposer.ts
class SceneComposer {
  async renderScene(sceneDefinition: SceneDef): Promise<void>
  getCharacterSprite(charDef: CharDef): HTMLImageElement
  cropSprite(sheet: HTMLImage, frameIndex: number): HTMLImageElement
}

// 테스트:
// ✓ 장면 렌더링 60fps 유지
// ✓ 스프라이트 시트 크롭 정확도
// ✓ 레이어 합성 순서 정확
```

**Task 1.3: 장면 정의 JSON 구조**
```json
// 파일: frontend/public/scenes/episode1_scenes.json
{
  "episodes": [
    {
      "episode": 1,
      "scenes": [
        {
          "scene": 1,
          "background": "bg_island_daytime",
          "characters": [...],
          "ui": [...],
          "audio": {...}
        }
      ]
    }
  ]
}

// 테스트:
// ✓ JSON 파싱 성공
// ✓ 모든 참조 유효
// ✓ 로드 시간 < 500ms
```

**Task 1.4: 기존 story.html 마이그레이션**
```typescript
// 파일: frontend/public/story.html (리팩토링)

// Before: 이미지 직접 경로
<img src="/episode/1/opening.jpg" ...>

// After: 에셋 시스템 사용
<canvas id="game-canvas"></canvas>
<script>
  const scene = await assetManager.renderScene('ep1_scene1');
</script>

// 테스트:
// ✓ 기존 기능 동일하게 작동
// ✓ 성능 개선 (캐싱 덕분)
// ✓ 모든 장면 표시 정확
```

**완료 조건**:
- [ ] AssetManager 클래스 완성 및 테스트
- [ ] SceneComposer 클래스 완성 및 테스트
- [ ] 모든 에셋 로드 성공
- [ ] story.html 마이그레이션 완료
- [ ] E2E 테스트 16개 모두 통과 (기존 16개 + 새로운 에셋 렌더링 4개)

#### Sprint 2: 뉴로-너굴 AI 기본 버전 (1주)

**목표**: Claude API 통합 및 기본 피드백 생성

**Task 2.1: Claude API 통합**
```typescript
// 파일: backend/src/services/NookAIService.ts
import Anthropic from "@anthropic-ai/sdk";

class NookAIService {
  async generateFeedback(context: FeedbackContext): Promise<string>
  async generateSuccess(context: SuccessContext): Promise<string>
  async generateHint(errorType: string): Promise<string>
}

// 테스트:
// ✓ API 연결 성공
// ✓ 응답 시간 < 1초
// ✓ 응답 내용 유효성 검사
```

**Task 2.2: 응답 캐싱**
```typescript
// 파일: backend/src/services/FeedbackCache.ts
class FeedbackCache {
  async getFeedback(key: string): Promise<string | null>
  setCachedFeedback(key: string, feedback: string): void
  clearOldEntries(): Promise<void>
}

// 테스트:
// ✓ 캐시 히트율 > 50%
// ✓ 캐시 미스 시 API 호출
// ✓ TTL 만료 자동 정리
```

**Task 2.3: API 엔드포인트**
```typescript
// 엔드포인트: POST /api/nook/feedback
router.post('/nook/feedback', async (req, res) => {
  const { episode, step, playerCode, errorType, isSuccess } = req.body;
  const feedback = await nookAI.generateFeedback({...});
  res.json({ feedback, expression, soundEffect });
});

// 테스트:
// ✓ 모든 매개변수 검증
// ✓ 에러 처리
// ✓ 응답 형식 일관성
```

**Task 2.4: 프론트엔드 통합**
```typescript
// 파일: frontend/src/services/NookAIClient.ts
class NookAIClient {
  async requestFeedback(context: FeedbackContext): Promise<Feedback>
}

// 사용 예시:
const feedback = await nookAI.requestFeedback({
  code: userCode,
  errorType: validation.errorType,
  isSuccess: validation.isValid
});

displayNookFeedback(feedback);

// 테스트:
// ✓ 요청-응답 사이클 < 2초
// ✓ 네트워크 오류 처리
// ✓ 다중 요청 큐잉
```

**완료 조건**:
- [ ] Claude API 성공적으로 통합
- [ ] 응답 캐싱 50% 이상 히트율
- [ ] API 엔드포인트 모든 테스트 통과
- [ ] 프론트엔드 통합 완료
- [ ] 실제 사용자 피드백 테스트 (10명 학생)

---

## 🧪 Phase 3.2: 고도화 (1주)

### Week 4: 성능 최적화 및 확대

**목표**: 시스템 안정화 및 에러 타입 확대

#### Sprint 3: 성능 최적화

**Task 3.1: 응답 시간 최적화**
```
목표: 평균 응답 시간 < 1초

현재: 1-2초 (API 호출 대기)
└─ 캐싱 적용 후: 50-100ms

측정:
- 캐시 히트: 50ms
- 캐시 미스: 1000-1500ms
- 목표 달성도: 캐시 히트율 > 60%
```

**Task 3.2: 동시 요청 처리**
```typescript
// 프론트엔드: 최대 3개까지만 동시 요청
class RequestQueue {
  private queue: Task[] = [];
  private active = 0;
  private maxConcurrent = 3;

  async enqueue(task: Task): Promise<Result>
  private async process(): Promise<void>
}

// 테스트:
// ✓ 최대 3개 동시 요청
// ✓ 큐 오버플로우 없음
// ✓ 요청 순서 보장
```

**Task 3.3: 에러 타입 확대**
```
기존 (MVP):
- MISSING_TYPE
- TYPE_MISMATCH_NO_CAST
- SYNTAX_ERROR

추가 (확대):
- WRONG_TYPE_SELECTION
- INCORRECT_OPERATOR
- MISSING_SEMICOLON
- UNDEFINED_VARIABLE
- INVALID_FUNCTION_CALL
- OFF_BY_ONE_ERROR
- LOGIC_ERROR

총 15가지 에러 타입
└─ 각각 3가지 피드백 배리에이션
└─ 총 45가지 기본 피드백
└─ AI가 학생 수준에 맞춰 맞춤형 생성
```

#### Sprint 4: 사용자 테스트

**Task 4.1: 5명 학생 파일럿 테스트**
```
진행:
1. 5명 학생 모집
2. Episode 1 완전 플레이
3. 피드백 수집
4. 문제점 정리

평가 지표:
- 만족도: 5점 만점
- 명확성: 피드백이 도움이 되는가?
- 자연스러움: 인간미가 있는가?
- 교육성: 배움의 기회가 되는가?

목표: 평균 4점 이상
```

**Task 4.2: 성능 데이터 수집**
```
모니터링:
- 응답 시간 분포
- 에러율
- API 비용
- 사용자 재시도율

목표:
- 평균 응답 시간 < 1초: 90% 달성
- 에러율 < 1%
- 월간 비용 < $10
- 재시도율 < 20%
```

**완료 조건**:
- [ ] 성능 목표 달성 (응답 < 1초)
- [ ] 에러 타입 15가지 대응
- [ ] 5명 학생 파일럿 테스트 완료
- [ ] 만족도 평가 4점 이상
- [ ] 모든 버그 수정

---

## 🚀 Phase 3.3: 확장 (2주)

### Week 5-6: Episode 2 제작 및 배포

**목표**: 모듈형 시스템의 이점 입증 및 대규모 배포

#### Sprint 5: Episode 2 제작 (1주)

**Task 5.1: Episode 2 스토리 작성**
```
주제: "무작위 수와 조건문 (Random & If/Else)"

시나리오:
- 해변의 나무 흔들기
- 돈이 떨어질까, 벌집이 떨어질까?
- Math.random() 학습
- if/else 조건문 학습

장면 구성:
- Scene 1: 너굴의 제안
- Scene 2: 나무 흔들기 설명
- IDE Mission 1: Math.random() 기본
- IDE Mission 2: 확률 계산 (50%)
- IDE Mission 3: if/else 조건문
- Scene 3: 결과 확인 및 축하

예상 제작 시간: 4일 (기존 방식 대비 84% 단축)
```

**Task 5.2: Episode 2 에셋 재사용**
```
배경:
- bg_island_daytime (재사용) ✓
- bg_island_forest (기존 포함 가능)

캐릭터:
- 너굴 스프라이트 (재사용) ✓
- 플레이어 (재사용) ✓

UI:
- 모든 기존 UI (재사용) ✓

신규 제작:
- 나무 이미지 (1장)
- 벌집 이미지 (1장)
- 돈 떨어지는 효과 애니메이션 (JSON)

신규 제작 시간: 4시간 (전체 제작 시간의 33%)
```

**Task 5.3: Episode 2 AI 피드백 확장**
```
새로운 에러 타입:
- RANDOM_FUNCTION_ERROR
- IF_CONDITION_ERROR
- COMPARISON_OPERATOR_ERROR
- PROBABILITY_CALCULATION_ERROR

각 에러별 3가지 피드백 배리에이션
└─ AI가 학생 수준에 맞춰 생성

예시:
"어? Math.random()이 뭔지 몰라?
나는 매번 '공짜인지 유료인지' 무작위로 정하는데,
그걸 프로그램으로 표현하는 거야!
random() 함수를 써봐!"
```

**완료 조건**:
- [ ] Episode 2 스토리 완성
- [ ] 에셋 재사용률 > 80%
- [ ] 신규 에셋 제작 < 1일
- [ ] 모든 IDE 미션 완성
- [ ] E2E 테스트 16개 추가 작성 및 통과

#### Sprint 6: 배포 및 모니터링

**Task 6.1: 프로덕션 배포**
```
배포 절차:
1. Staging 환경 테스트 (모든 테스트 통과)
2. 성능 벤치마크 검증
3. 보안 감사
4. 프로덕션 배포

배포 대상:
- Frontend: Vercel / Netlify
- Backend: AWS EC2 / Google Cloud
- Assets: CDN (CloudFlare)
- Cache: Redis 클라우드
```

**Task 6.2: 모니터링 설정**
```typescript
// 모니터링 항목:
1. API 응답 시간 (Datadog)
2. 에러율 (Sentry)
3. 사용자 만족도 (Google Analytics)
4. AI API 비용 (CloudWatch)

대시보드:
- 실시간 응답 시간
- 에러 추적
- 사용자 분포
- 비용 추이
```

**Task 6.3: 사용자 피드백 수집**
```
방식:
- 게임 내 설문조사 (매 에피소드 후)
- NPS (Net Promoter Score)
- 오픈 피드백 (자유 의견)

목표:
- 만족도 > 80%
- 추천의향도 > 70%
- 재방문율 > 60%
```

**완료 조건**:
- [ ] 프로덕션 배포 성공
- [ ] 모니터링 대시보드 구성
- [ ] 100명 이상 사용자 테스트
- [ ] 만족도 80% 이상
- [ ] AI 비용 < $50/월

---

## 📊 리소스 계획

### 인력 투입

```
┌─────────────────────────────────────────┐
│ 팀 구성 (3-4명 권장)                   │
├─────────────────────────────────────────┤
│                                         │
│ 🔧 백엔드 엔지니어 (1명)               │
│   └─ AI 통합, API 개발                 │
│                                         │
│ 🎨 프론트엔드 엔지니어 (1명)           │
│   └─ 에셋 시스템, UI 구현              │
│                                         │
│ 🎨 디자인/에셋팀 (0.5명)               │
│   └─ 스프라이트, UI 에셋               │
│                                         │
│ 📝 교육 콘텐츠팀 (0.5명)               │
│   └─ AI 페르소나, 스토리               │
│                                         │
│ 🧪 QA 엔지니어 (0.5명)                │
│   └─ 테스트, 모니터링                  │
│                                         │
│ 🎯 PM (0.5명)                         │
│   └─ 조율, 일정 관리                   │
│                                         │
└─────────────────────────────────────────┘
```

### 비용 추정

```
개발 비용:
- 인건비 (6주 × 4명): $48,000 (월 $8,000/인)
- 도구 구독: $1,000 (Claude API, 클라우드 등)
- 인프라: $2,000 (호스팅, 데이터베이스 등)
├─ 소계: $51,000

운영 비용 (월):
- API 비용: $50
- 호스팅: $200
- 모니터링: $100
├─ 월 소계: $350

ROI 분석:
- 개발 비용: $51,000
- 연간 운영 비용: $4,200
- 1000명 학생 × $50 구독료 = $50,000/년
└─ 1년 후 손익분기점 도달 가능
```

---

## ✅ 체크리스트

### Phase 3.0 (1주)
- [ ] 에셋 리스트 최종 확정
- [ ] 너굴 AI 페르소나 완성
- [ ] 기술 스택 선정
- [ ] 팀 구성 완료
- [ ] 개발 환경 설정

### Phase 3.1 (2주)
- [ ] AssetManager 완성
- [ ] SceneComposer 완성
- [ ] Claude API 통합
- [ ] 기본 피드백 15가지
- [ ] E2E 테스트 작성

### Phase 3.2 (1주)
- [ ] 성능 최적화 (< 1초)
- [ ] 에러 타입 15가지
- [ ] 5명 파일럿 테스트
- [ ] 만족도 4점 이상
- [ ] 모든 버그 수정

### Phase 3.3 (2주)
- [ ] Episode 2 완성
- [ ] 에셋 재사용 > 80%
- [ ] 프로덕션 배포
- [ ] 모니터링 구성
- [ ] 100명 사용자 테스트

---

## 🎯 성공 지표

```
기술 지표:
✅ 응답 시간 < 1초 (캐시 히트율 > 60%)
✅ 에러율 < 1%
✅ AI 비용 < $50/월
✅ 캐싱 히트율 > 60%

교육 지표:
✅ 학생 만족도 > 80%
✅ 에러 이해도 > 85% (사후 평가)
✅ 재방문율 > 60%
✅ 에피소드 완료율 > 70%

비즈니스 지표:
✅ 개발 시간 80% 단축 (에피소드당)
✅ 에셋 재사용 > 80%
✅ 비용 절감 > 70% (장기)
✅ ROI 1년 이내 달성
```

---

## 🚀 즉시 시작 가능 단계

**현재 상태**: Phase 3.0 - 준비 단계
**즉시 시작 가능**: 다음주 월요일

### Week 1 실행 계획

| 요일 | 작업 | 책임자 |
|------|------|--------|
| 월 | 에셋 리스트 최종 확정 | 디자인팀 |
| 화 | AI 페르소나 프롬프트 완성 | 교육팀 |
| 수 | 기술 스택 검토 및 선정 | 개발팀 |
| 목 | 팀 킥오프 미팅 | PM |
| 금 | 개발 환경 구성 | 백엔드팀 |

---

**상태**: ✅ 구현 로드맵 설계 완료
**다음**: 즉시 실행 가능 (팀 확보 후)
**예상 완료**: 6주
