# 너굴 코딩 플랫폼 - DoD (Definition of Done) 체크리스트

## 📋 개요

DoD(Definition of Done)는 **"기능/작업이 언제 진정으로 완료되었는가?"**를 정의하는 체크리스트입니다.

이 문서는 기능 단위, 마일스톤 단위, 출시 단위로 총 3가지 DoD를 제시합니다.

---

## 1️⃣ 기능 단위 DoD (Feature-Level DoD)

### 목적
단일 기능(예: "코드 실행 기능")이 개발 완료되었는지 판단

### 체크리스트

#### 🔧 개발 (Development)
- [ ] 코드 작성 완료
- [ ] 로컬에서 정상 작동 확인
- [ ] 메인 브랜치와 충돌 없음 (no merge conflicts)
- [ ] 코드 스타일 준수 (Prettier, ESLint 통과)
- [ ] TypeScript 타입 검사 통과 (`npm run type-check`)
- [ ] 불필요한 console.log, debugger 제거
- [ ] 환경 변수 하드코딩 제거

#### ✅ 단위 테스트 (Unit Testing)
- [ ] 단위 테스트 작성 (최소 80% 커버리지)
  - 주요 로직: 100%
  - 유틸리티 함수: 80% 이상
  - UI 컴포넌트: 60% 이상
- [ ] 모든 단위 테스트 통과 (`npm run test:unit`)
- [ ] Edge case 테스트 포함 (null, undefined, 빈 배열 등)
- [ ] 오류 케이스 테스트 포함

**예시: 코드 실행 엔진**:
```typescript
// ✅ 필수 테스트
- executeCode("int x = 5;") → success
- executeCode("int x;") → error
- executeCode(null) → error
- executeCode("") → error
- executeCode(매우_긴_코드_1000줄) → timeout or success
```

#### 🔀 통합 테스트 (Integration Testing)
- [ ] 통합 테스트 작성 (주요 흐름)
- [ ] 모든 통합 테스트 통과
- [ ] 데이터베이스/API 연동 확인
- [ ] 트랜잭션 롤백 시나리오 테스트

**예시: 게임 상태 업데이트**:
```typescript
// ✅ 필수 테스트
1. 코드 실행 → API 호출 → DB 저장 → UI 업데이트
2. 실행 중 에러 발생 → 상태 롤백
3. 동시 실행 → 데이터 일관성 유지
```

#### 📚 정적 분석 (Static Analysis)
- [ ] ESLint 통과 (에러 0개, 경고 최소화)
- [ ] TypeScript 에러 0개
- [ ] SonarQube 심각도 높음(Critical) 0개
- [ ] 보안 취약점 검사 통과 (`npm audit fix`)

#### 📖 문서화 (Documentation)
- [ ] 코드 주석 (복잡한 로직만, 명확한 코드는 불필요)
  ```typescript
  // ❌ 불필요한 주석
  let x = 5; // x에 5 할당

  // ✅ 필요한 주석
  // User Feedback Algorithm:
  // Deduct 2 points if code uses 'while' instead of 'for'
  // This encourages learning the preferred loop construct
  let score = calculateScore(code);
  ```
- [ ] 함수 JSDoc 주석
  ```typescript
  /**
   * Executes student code against test cases
   * @param code - The student's Java code
   * @param testCases - Array of test cases to validate
   * @returns ExecutionResult with success status and output
   * @throws CodeExecutionError if compilation fails
   */
  export async function executeCode(
    code: string,
    testCases: TestCase[]
  ): Promise<ExecutionResult>
  ```
- [ ] README 업데이트 (해당시)
- [ ] API 변경사항 문서화 (Swagger/OpenAPI)

#### 🎯 코드 리뷰 (Code Review)
- [ ] 다른 개발자의 코드 리뷰 승인 (최소 1명)
- [ ] 모든 리뷰 코멘트 해결
- [ ] PR 설명이 명확함 (변경사항, 테스트 방법 포함)

**PR 템플릿 예시**:
```markdown
## 변경 사항
- 코드 실행 API 백엔드 구현

## 이유
Unit 1 미션 완료를 위해 필수

## 테스트 방법
1. `npm run test:unit` - 모두 통과
2. `npm run test:integration` - 모두 통과
3. `curl -X POST http://localhost:5000/api/code/execute` 수동 테스트

## 체크리스트
- [x] 단위 테스트 작성
- [x] 통합 테스트 작성
- [x] ESLint 통과
- [x] TypeScript 통과
- [x] 문서화 완료
```

#### 🚀 배포 준비 (Deployment Ready)
- [ ] 로컬에서 최종 테스트
- [ ] 환경 변수 확인 (.env 파일 준비)
- [ ] 데이터베이스 마이그레이션 (해당시)
- [ ] 버그 Hotfix 필요 없음 (Bug List 확인)

---

## 2️⃣ 마일스톤 단위 DoD (Milestone-Level DoD)

### 목적
한 주기(Sprint, Phase) 동안의 모든 기능이 조화롭게 작동하는지 판단

### 마일스톤 구성

#### 예시: "Phase 1: MVP 코드 실행 엔진"
목표 기간: 2주

**구현 범위**:
1. 백엔드 코드 실행 API
2. 프론트엔드 IDE 연동
3. 기본 게임 상태 관리
4. 3개 샘플 미션

### 체크리스트

#### 📦 기능 완성도 (Feature Completion)
- [ ] 모든 계획된 기능 구현 완료
- [ ] 각 기능이 DoD 통과
- [ ] 기능 간 통합 테스트 통과

**예시**:
```
Phase 1 기능:
[✅] 코드 실행 API
[✅] IDE 프론트엔드
[✅] 게임 상태 기본값
[✅] 3개 미션 (변수, 조건문, 루프)
```

#### 🧪 E2E 테스트 (End-to-End)
- [ ] 주요 사용자 시나리오 E2E 테스트 통과
  - 로그인 → 미션 선택 → 코드 작성 → 실행 → 피드백 → 계속
- [ ] 모든 중요 경로(Happy Path) 테스트
- [ ] 오류 처리 경로(Error Path) 테스트

**예시 시나리오**:
```typescript
describe('Phase 1 E2E Tests', () => {
  test('User completes Mission 1: Variables', async () => {
    await login('student1');
    await selectMission('m_01_variables');
    await writeCode('int x = 10;');
    await runCode();
    expect(result).toHaveSuccessMessage();
    expect(gameState.bells).toBe(5000);
  });

  test('User sees error for incorrect code', async () => {
    await login('student1');
    await selectMission('m_01_variables');
    await writeCode('int x;'); // 초기화 없음
    await runCode();
    expect(result).toHaveErrorMessage();
    expect(result.hint).toBeDefined();
  });
});
```

#### 📊 성능 테스트 (Performance Baseline)
- [ ] 응답 시간 < 500ms (p95)
- [ ] 동시 사용자 10명 테스트 통과
- [ ] 메모리 누수 없음 (Chrome DevTools 확인)
- [ ] 번들 크기 < 1MB (프론트엔드 기본)

**성능 측정**:
```bash
# 성능 테스트 실행
npm run perf:test

# 결과 예시
API Response Time: 234ms (p95)
Frontend Load Time: 1.2s
Memory Usage: 145MB
```

#### 🔒 보안 검사 (Security)
- [ ] npm audit 결과: Critical 0, High 0
- [ ] 환경 변수 노출 검사
- [ ] SQL Injection 방지 검증
- [ ] XSS 방지 검증
- [ ] CSRF 토큰 확인 (필요시)

#### 📖 문서화 (Documentation)
- [ ] Phase 완료 서무 작성
  - 구현된 기능 목록
  - 알려진 이슈
  - 다음 Phase 계획
- [ ] API 문서 최신화
- [ ] 아키텍처 다이어그램 업데이트
- [ ] 설정 가이드 (`.env` 예시, DB 마이그레이션 등)

**문서 예시**:
```markdown
# Phase 1 Completion Report

## 구현된 기능
- [x] Code Execution API
- [x] IDE Window Component
- [x] Game State Redux Store
- [x] 3 Sample Missions

## 테스트 결과
- Unit Tests: 45/45 통과 ✅
- Integration Tests: 12/12 통과 ✅
- E2E Tests: 5/5 통과 ✅
- Performance: API < 500ms ✅

## 알려진 이슈
- [ ] 매우 긴 코드(5000줄)는 타임아웃 가능
- [ ] iOS Safari에서 대화 시스템 폰트 깨짐

## 다음 Phase
- TileGrid 렌더러 구현
- Dialogue System 완성
- 정적 분석 추가
```

#### 🎯 릴리스 준비 (Release Readiness)
- [ ] 릴리스 노트 작성
- [ ] 버전 번호 업데이트 (Semantic Versioning)
- [ ] 태그 생성 (`git tag v0.1.0-alpha`)
- [ ] 핫픽스 브랜치 준비

---

## 3️⃣ 출시 단위 DoD (Release-Level DoD)

### 목적
프로덕션 배포 전 최종 점검

### 출시 유형별 DoD

#### 🔴 Major Release (v1.0.0)
새로운 기능 세트 또는 아키텍처 변경

**체크리스트**:
- [ ] 모든 Phase 완료
- [ ] 모든 마일스톤 통과
- [ ] 전체 통합 테스트 통과
- [ ] 사용자 수용 테스트(UAT) 완료
- [ ] 성능/보안 감사 통과
- [ ] 문서 100% 업데이트
- [ ] 마이그레이션 가이드 (해당시)
- [ ] 롤백 계획 수립

**예시**:
```
v1.0.0: "너굴 코딩 - 완전한 게임화 플랫폼"
- 12개 미션 (모든 Unit 커버)
- 완전한 게임 시스템 (경제, 인벤토리, 관계도)
- 스토리라인 연동
```

#### 🟡 Minor Release (v1.1.0)
새로운 기능 추가 (기존 기능과 호환)

**체크리스트**:
- [ ] 새 기능 DoD 통과
- [ ] 기존 기능과의 호환성 테스트 통과
- [ ] 회귀 테스트(Regression Test) 통과
- [ ] 성능 저하 없음
- [ ] 마이그레이션 스크립트 (필요시)

#### 🟢 Patch Release (v1.0.1)
버그 수정 또는 소규모 개선

**체크리스트**:
- [ ] 버그 수정 검증
- [ ] 회귀 테스트 통과
- [ ] 무결성 테스트 통과

### 배포 전 최종 체크리스트

#### 🖥️ 인프라 준비 (Infrastructure)
- [ ] 프로덕션 서버 상태 확인
- [ ] 데이터베이스 백업 (스냅샷 생성)
- [ ] CDN 캐시 설정
- [ ] DNS/로드 밸런서 설정
- [ ] 모니터링/로깅 활성화

#### 📱 클라이언트 준비 (Client Setup)
- [ ] 브라우저 호환성 최종 확인
- [ ] 모바일 반응형 테스트
- [ ] 오프라인 모드 (있으면) 테스트
- [ ] PWA/서비스 워커 (필요시)

#### 🚨 모니터링 준비 (Monitoring)
- [ ] Sentry/로그 수집 활성화
- [ ] 성능 모니터링(Datadog) 활성화
- [ ] 알림 규칙 설정
  - CPU > 80%
  - 메모리 > 90%
  - 오류율 > 1%
  - API 응답시간 > 1초
- [ ] Slack/이메일 알림 채널 확인

#### 📢 커뮤니케이션 (Communication)
- [ ] 출시 공지 준비
- [ ] 사용자 가이드 준비
- [ ] FAQ/트러블슈팅 가이드
- [ ] 팀 내 배포 일정 공지

#### ✅ 최종 승인 (Final Approval)
- [ ] 기술 리더 승인
- [ ] 프로덕션 담당자 승인
- [ ] QA 최종 승인
- [ ] (선택) 기획/비즈니스 승인

---

## 📋 DoD 템플릿 (PR/Issue 사용)

### GitHub Issue Template

```markdown
# [Feature] 기능명

## 설명
이 기능이 무엇인지 설명해주세요.

## 수용 기준 (Acceptance Criteria)
- [ ] 요구사항 1
- [ ] 요구사항 2
- [ ] 요구사항 3

## 완료 체크리스트 (DoD)

### 개발
- [ ] 코드 작성 완료
- [ ] 로컬 테스트 통과
- [ ] 스타일 검사 통과

### 테스트
- [ ] 단위 테스트 작성 (80%+ 커버리지)
- [ ] 통합 테스트 작성
- [ ] 모든 테스트 통과

### 문서화
- [ ] 함수/클래스 주석 작성
- [ ] README 업데이트

### 검토
- [ ] 코드 리뷰 승인
- [ ] 모든 리뷰 의견 해결

---

## 리뷰 관점

### Code Review Checklist
- [ ] 코드 읽기 쉬운가?
- [ ] 테스트 충분한가?
- [ ] 문서 명확한가?
- [ ] 성능 문제는 없는가?
- [ ] 보안 취약점은 없는가?

```

### GitHub PR Template

```markdown
# [PR] PR 제목

## 변경사항
- 항목 1
- 항목 2

## 링크
Closes #123

## 테스트 방법
1. 단계 1
2. 단계 2
3. 단계 3

## 스크린샷/영상 (해당시)
[이미지/비디오 첨부]

## DoD 체크리스트
- [ ] 단위 테스트 통과
- [ ] 통합 테스트 통과
- [ ] ESLint 통과
- [ ] TypeScript 통과
- [ ] 문서 업데이트
```

---

## 🎯 DoD 적용 흐름

```
개발자 작업 시작
    ↓
feature 브랜치에서 개발
    ↓
기능 단위 DoD 체크
    ↓
PR 생성 + 코드 리뷰
    ↓
모든 리뷰 해결
    ↓
PR 머지 (main/develop)
    ↓
마일스톤 DoD 체크 (주기별)
    ↓
출시 계획 수립
    ↓
출시 DoD 체크
    ↓
프로덕션 배포
    ↓
모니터링 (1주일)
```

---

## 📊 DoD 준수율 추적

### 주간 리포트

```
주간 DoD 준수율 (Week of Dec 4-10)

기능별:
┌─────────────────────────────┬─────┐
│ 기능명                      │ DoD │
├─────────────────────────────┼─────┤
│ Code Execution API          │ 100%│
│ IDE Window Component        │ 95% │ ⚠️ 1개 리뷰 코멘트 해결 중
│ Redux Store Setup           │ 100%│
└─────────────────────────────┴─────┘

마일스톤별:
Phase 1: 85% (E2E 테스트 작성 중)

전체: 92% ✅
```

### DoD 준수율 > 95% 유지 목표

---

## 💡 Best Practices

### ✅ DO (해야 할 것)
- 작은 단위의 작업 (PR은 200줄 이하)
- 명확한 체크리스트
- 정기적인 리뷰 (24시간 이내)
- 테스트 우선 (TDD)
- 진행 상황 투명하게 공유

### ❌ DON'T (하지 말아야 할 것)
- 거대한 PR (1000줄+)
- DoD 우회 ("나중에 테스트 작성하겠습니다")
- 문서화 생략
- 테스트 없이 병합
- 승인 없이 배포

